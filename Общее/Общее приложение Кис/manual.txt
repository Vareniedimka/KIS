нужно будет создать пользователей для бд это информация зависит не от
бд а от винды по этому придется ее вводить на каждом новом компе.
Поэтому лучше сделать скрипт и его выполнять при подключении.
>>>
/*Чтобы все было ок должна быть включена
 проверка подленности sql server и windows для это 
 можешь проверить в свойствах подключения/безопасность, 
 если поменял то нужно перезапустить sql (так же как при копировании бд)
*/
/*
так создается пользователь. До слеша это префикс подсистемы.
У меня управление запасами по этому UZ/ У остальных придумаете.
Короче у вас должен получится скрипт который создает много 
пользователей ну по одному для каждой подсистемы наверн саме решите
*/

CREATE LOGIN [UZ/Sergey] 
   WITH PASSWORD = '12345' 
go
USE [AvtoK];
CREATE USER [UZ/Sergey] FOR LOGIN [UZ/Sergey];
GRANT  DELETE, INSERT, REFERENCES, SELECT, UPDATE TO [UZ/Sergey];
GO

>>>
В приложении если приложение мое просто переделанное то нужно следующее 
1. Поменять класс Connect на такойже как у меня в исходниках.
>>> MainWindow
2. поменять метод public MainWindow() по аналогии с моим(по сути там 2 строчки добавить в начало) 
и убрать подключение
3. удалить окно с подключением.
4. bReload_Click убрать вызов окна подключения  также можно посмотреть как это у меня сделано
5. bConnect_Click сделать как у меня (удалить все и добавить  
	System.Diagnostics.Process.Start("LoginApp.exe");
	Close();
)
для остальных кто не использует мое приложение:
программа объединяющяя приложение передает вашему приложениб параметры для подключения
параметры можно получить функцией  
var arr = Environment.GetCommandLineArgs();
она возвращает массив строк где 
нулевой элемент вам не нужен
arr[1] - сервер
arr[2] - бд
arr[3] - имя пользователя 
arr[4] - пароль
	 Environment.GetCommandLineArgs(); -- 